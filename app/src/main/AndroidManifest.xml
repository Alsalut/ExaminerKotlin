<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_student"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_student"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExaminerKotlin"
        tools:targetApi="31">
        <activity
            android:name=".EditingActivity"
            android:exported="false" />
        <activity
            android:name=".ShowActivity"
            android:exported="false"
            android:label="@string/show_questions" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_question">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>