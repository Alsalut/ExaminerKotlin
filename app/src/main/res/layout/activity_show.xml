<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/style_constraint_layout"
    tools:context=".ShowActivity">

    <TextView
        android:id="@+id/text_view_activity_show"
        style="@style/style_text_view"
        android:gravity="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@android:id/list"
        style="@style/style_list_view"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/image_button_plus_activity_show"
        style="@style/style_image_button"
        android:background="@drawable/background_image_button"
        android:src="@drawable/btn_plus_black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/image_button_next_activity_show"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/image_button_next_activity_show"
        style="@style/style_image_button"
        android:background="@drawable/background_image_button"
        android:src="@drawable/btn_next_black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/image_button_plus_activity_show"
        tools:ignore="ContentDescription" />

</androidx.constraintlayout.widget.ConstraintLayout>